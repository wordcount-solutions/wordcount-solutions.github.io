# Zola configuration for GitHub Pages preview
# This file uses a relative base_url for GitHub Pages
# Copy this to config.toml temporarily for GitHub Pages preview, or use environment variable

# For GitHub Pages project pages, base_url should be "/repository-name/"
# For GitHub Pages user/org pages, base_url should be "/"
base_url = "/wordcount-website/"

# Copy the rest of your config.toml settings here, or use this as a template
# and merge with your production config.toml

